pipeline {
  agent any

  stages {
    stage('Build Docker Image') {
      steps {
        sh 'docker build -t todo .'
      }
    }

    stage('Deploy Container') {
      steps {
        sh 'docker run -d --name todoapp -p 8000:8000 todo'
      }
    }
  }
}
